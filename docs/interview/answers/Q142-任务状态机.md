# Q142: 任务状态机：PENDING → RUNNING → COMPLETED，什么事件触发状态变更？

## 元信息

| 字段 | 内容 |
|------|------|
| 编号 | Q142 |
| 分类 | 后端工程与 API 设计 |
| 难度 | ★ |

## 问题

任务状态机：PENDING → RUNNING → COMPLETED，什么事件触发状态变更？

## 参考答案

围绕 任务状态机：PENDING→RUNNING→COMPLETED 面试回答应先说业务场景，再落到 app/db/models.py 的实现细节与配置项（app/core/config.py 中 EVAL_PARALLEL、EVAL_BATCH_SIZE 等）。FastAPI 异步 + SQLAlchemy 2.0；POST /evaluations 返回 202，/evaluations/stream 提供 SSE progress。若涉及可靠性，提 monotonicity benchmark 容差 +0.05 与 ReplanEvaluator 无 missed 时满分 100 的规则。

## 代码依据

- `app/db/models.py`
- `app/graphs/evaluation_graph.py`
- `app/evaluators/base.py`

## 回答要点

- 任务状态机：PENDING→RUNNING→COMPLETED
- 代码入口：app/db/models.py
- 与六维 LLM-as-Judge 评估链路相关
- 轨迹 schema 见 app/models/action_types.py

## 常见追问

**Q: 「任务状态机」最先看哪段代码？**

A: 打开 app/db/models.py，再对照 app/graphs/evaluation_graph.py 的数据流。

**Q: Demo 里如何验证 任务状态机？**

A: 跑 Wiki Agent + EVAL_AUTO_RUN，或 sdk/collector finish(auto_run=True)。

**Q: 与 benchmark 关系？**

A: 改相关 Evaluator 后跑 app/benchmarks/monotonicity.py 看是否仍单调。

## 相关题目

- [Q141](../answers/Q141-SSE-replay.md)
- [Q143](../answers/Q143-PENDING与RUNNING.md)
