# Q112: 如何构造「检索好但生成差」和「检索差但生成碰巧对」的测试用例？

## 元信息

| 字段 | 内容 |
|------|------|
| 编号 | Q112 |
| 分类 | RAG 与检索质量 |
| 难度 | ★ |

## 问题

如何构造「检索好但生成差」和「检索差但生成碰巧对」的测试用例？

## 参考答案

围绕 检索好生成差：测例：高 relevance 低 evidence_accuracy 面试回答应先说业务场景，再落到 app/evaluators/retrieval_evaluator.py 的实现细节与配置项（app/core/config.py 中 EVAL_PARALLEL、EVAL_BATCH_SIZE 等）。Wiki 侧 hybrid_search 用 Milvus 语义 + jieba BM25，RRF k=60 融合；检索步骤需 record_retrieval 供 RetrievalEvaluator。若涉及可靠性，提 monotonicity benchmark 容差 +0.05 与 ReplanEvaluator 无 missed 时满分 100 的规则。

## 代码依据

- `app/evaluators/retrieval_evaluator.py`
- `app/graphs/evaluation_graph.py`
- `app/evaluators/base.py`

## 回答要点

- 检索好生成差：测例：高 relevance 低 evidence_accuracy
- 代码入口：app/evaluators/retrieval_evaluator.py
- 与六维 LLM-as-Judge 评估链路相关
- 轨迹 schema 见 app/models/action_types.py

## 常见追问

**Q: 「检索好生成差」最先看哪段代码？**

A: 打开 app/evaluators/retrieval_evaluator.py，再对照 app/graphs/evaluation_graph.py 的数据流。

**Q: Demo 里如何验证 检索好生成差？**

A: 跑 Wiki Agent + EVAL_AUTO_RUN，或 sdk/collector finish(auto_run=True)。

**Q: 与 benchmark 关系？**

A: 改相关 Evaluator 后跑 app/benchmarks/monotonicity.py 看是否仍单调。

## 相关题目

- [Q111](../answers/Q111-record-retrieval.md)
- [Q113](../answers/Q113-RAG-ground-truth.md)
