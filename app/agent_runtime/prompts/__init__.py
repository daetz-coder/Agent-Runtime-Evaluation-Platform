"""Prompt version stamp for evaluation metadata.

The sandbox Agent system-prompt manager was removed; only the version
string remains so evaluation records can record which rubrics era they used.
"""

from __future__ import annotations

PROMPT_VERSION = "v1.1"
